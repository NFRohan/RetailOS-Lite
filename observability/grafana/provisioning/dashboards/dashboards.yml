apiVersion: 1

providers:
  - name: RetailOS
    orgId: 1
    folder: RetailOS Lite
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
